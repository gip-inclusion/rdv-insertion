<%= link_to rdv_context_closings_path(rdv_context.id), id: "rdv-context-closings-#{rdv_context.id}", data: { turbo_frame: "user_page" }, method: :post do %>
  <button class="btn btn-blue-out" id="rdv-context-closings-<% dom_id(rdv_context) %>" data-controller="tooltip" data-action="mouseover->tooltip#closeRdvContextButton">
    <i class="fas fa-lock"></i> Clôturer
  </button>
<% end %>
