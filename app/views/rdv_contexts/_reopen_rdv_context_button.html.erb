<%= link_to rdv_context_closings_path(rdv_context.id), id: "rdv-context-closings-#{rdv_context.id}", data: { turbo_frame: "user_page" }, method: :delete do %>
  <button class="btn btn-blue"  data-controller="tooltip" data-action="mouseover->tooltip#reopenRdvContextButton">
    <i class="fas fa-lock-open"></i> Rouvrir "<%= rdv_context.motif_category.name %>"
  </button>
<% end %>
