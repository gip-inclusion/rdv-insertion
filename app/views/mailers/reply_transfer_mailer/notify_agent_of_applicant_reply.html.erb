<h1>Bonjour,</h1>
<p>Vous trouverez ci-dessous une réponse d'un.e bénéficiaire à une <%= @rdv ? "convocation" : "invitation" %>:</p>
<%= render "quote_original_mail" %>
<p>Merci de ne pas répondre à cet e-mail. Pour contacter la personne, vous pouvez utiliser les informations contenues dans cet e-mail.</p>
<blockquote>
  <h4>Éxpéditeur</h4>
  <%= tag.p(@applicant.full_name) if @applicant %>
  <%= tag.p(@applicant ? @applicant.email : @author) %>
  <%= tag.p(@applicant.phone_number) if @applicant&.phone_number %>
  <br />
  <h4>Message envoyé par rdv-insertion</h4>
  <%= render "rdv_informations" if @rdv %>
  <%= render "invitation_informations" if @invitation %>
</blockquote>
<% if @applicant %>
  <p class="btn-wrapper">
    <%= link_to organisation_applicant_url(@organisation, @applicant, host: ENV['HOST']), class: "btn btn-primary" do %>
      Voir la fiche usager
    <% end %>
  </p>
<% end %>
