<% if user && department %>
  <p class="btn-wrapper">
    <%= link_to department_user_url(department, user, host: ENV['HOST']), class: "btn btn-primary" do %>
      Voir la fiche usager
    <% end %>
  </p>
<% end %>
