<%= render "common/remote_modal", title: "Autorisez à exporter des fichiers CSV" do %>
  <%= render "form", organisation_id: @organisation.id, agent_roles: @agent_roles, authorized_agent_role_ids: @authorized_agent_role_ids %>
<% end %>
