<% if @success %>
  <%= turbo_stream.replace dom_id(@participation, :status), partial: "participations/participation_status", locals: { participation: @participation } %>
  <%= turbo_stream.replace dom_id(@participation.follow_up, :status_tag), partial: "follow_ups/follow_up_status_badge", locals: { follow_up: @participation.follow_up, category_configuration: @participation.current_category_configuration } %>
<% end %>
<%= render_turbo_stream_flash_messages %>
