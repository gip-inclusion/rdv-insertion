<%= turbo_frame_tag "category_configurations_list" do %>
  <%= render "list",
             category_configurations: @category_configurations,
             motifs_by_category: @motifs_by_category,
             organisation: @organisation %>
<% end %>
