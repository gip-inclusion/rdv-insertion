<% if @configuration.present? %>
  <%= react_component(
    "pages/ApplicantsUpload", {
      organisation: @organisation, configuration: @configuration, department: @department, contextName: @context_name
    }
  ) %>
<% else %>
  <%= render "context_selection" %>
<% end %>
