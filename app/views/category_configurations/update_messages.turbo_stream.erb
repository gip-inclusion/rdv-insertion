<%= turbo_stream.replace dom_id(@category_configuration, :messages) do %>
  <%= render "category_configurations/messages",
             category_configuration: @category_configuration,
             organisation: @organisation %>
<% end %>
