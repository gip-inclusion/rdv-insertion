<%= turbo_stream.replace dom_id(@category_configuration, :notifications) do %>
  <%= render "category_configurations/notifications",
             category_configuration: @category_configuration,
             organisation: @organisation %>
<% end %>
