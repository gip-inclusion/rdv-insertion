<%= react_component(
  "pages/users/Uploads", {
    organisation: @organisation,
    categoryConfiguration: @current_category_configuration,
    columnNames: @file_configuration.column_attributes,
    sheetName: @file_configuration.sheet_name,
    department: @department,
    motifCategoryName: @motif_category_name,
    currentAgent: current_agent,
    tags: (@organisation || @department).tags
  }
) %>
