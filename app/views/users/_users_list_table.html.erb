<div class="col-12 text-center overflow-scroll">
  <% if no_search_results?(@users.to_a) %>
    <h4 class="py-4">Aucun usager ne correspond à votre recherche</h4>
  <% elsif archived_scope?(@users_scope) %>
    <%= render "archived_users_table" %>
  <% elsif @current_configuration %>
    <%= render "users_table_for_motif_category" %>
  <% else %>
    <%= render "all_users_table" %>
  <% end %>
  <%= render "users_list_pagination" %>
</div>
