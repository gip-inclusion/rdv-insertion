<%= render "common/remote_modal", title: "Choisissez un ou plusieurs tag(s)" do %>
  <%= render "form", assignable_tags: @available_tags - @user_tags, assigned_tags: @user_tags, user_id: @user.id %>
<% end %>
