<% content_for :title, "Envoyer des invitations aux usagers non-invités - #{@organisation&.name || @department.name} - #{@motif_category_name} - rdv-insertion" %>

<%= react_component(
  "pages/users/BatchActions", {
    usersFromApp: @users,
    organisation: @organisation,
    categoryConfiguration: @current_category_configuration,
    department: @department,
    motifCategoryName: @motif_category_name,
    currentAgent: current_agent,
    backToUsersListUrl: @back_to_users_list_url
  }
) %>
