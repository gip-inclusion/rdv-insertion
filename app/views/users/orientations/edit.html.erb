<%= render "common/remote_modal", title: "Modifier une orientation" do %>
  <%= form_for(@orientation, url: department_user_orientation_path(id: @orientation.id, user_id: @orientation.user_id, department_id: Current.department.id), method: :patch) do |form| %>
    <%= render "orientations/orientation_form", form:, orientation: @orientation, organisations: @organisations, agents: @agents %>
  <% end %>
<% end %>
