<% content_for :title, "Créer un usager - #{Current.structure.name} - rdv-insertion" %>

<%= form_for(@user, url: structure_users_path, method: :post) do |f| %>
  <%= render "user_form", f: f, return_path: structure_users_path %>
<% end %>

