<% if can_be_added_to_other_org %>
  <div id="add_to_org_button" class="row d-flex justify-content-start flex-wrap">
    <div class="col-12 col-md-6 px-5">
      <%= link_to(new_department_applicant_applicants_organisation_path(department, applicant, assign_rdv_context: true), data: { turbo_frame: 'remote_modal' }) do %>
        <button class="btn btn-blue"><i class="fas fa-plus"></i> Ajouter à une organisation</button>
      <% end %>
    </div>
  </div>
<% end %>
