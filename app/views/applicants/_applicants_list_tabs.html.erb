<ul class="nav nav-tabs">
  <% @all_configurations.each do |configuration| %>
    <li class="nav-item">
      <%= link_to configuration.motif_category_name, compute_index_path(Applicant, @organisation, @department, motif_category_id: configuration.motif_category_id), class: "nav-link #{configuration.id == @current_configuration&.id ? 'active' : ''}"%>
    </li>
  <% end %>
  <li class="nav-item">
    <%= link_to "Archivés", compute_index_path(Applicant, @organisation, @department, applicants_scope: "archived"), class: "nav-link #{archived_scope?(@applicants_scope) ? 'active' : ''}" %>
  </li>
</ul>
