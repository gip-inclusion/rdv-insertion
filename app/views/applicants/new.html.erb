<%= simple_form_for(@applicant, url: organisation_applicants_path(@organisation, @applicant), html: { method: :post }) do |f| %>
  <%= render 'applicant_form', f: f, page_name: "new", title: "Créer allocataire" %>
<% end %>

