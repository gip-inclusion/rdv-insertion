const enforceSafeSwal = require("./enforce-safe-swal");
const enforceSafeTippy = require("./enforce-safe-tippy");

module.exports = {
  rules: {
    "enforce-safe-swal": enforceSafeSwal,
    "enforce-safe-tippy": enforceSafeTippy
  }
};