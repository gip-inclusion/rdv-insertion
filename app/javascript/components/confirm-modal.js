import { Modal } from "bootstrap";

class ConfirmModal {
  constructor() {
    window.Turbo.setConfirmMethod(this.confirm.bind(this));
    this.element = document.querySelector("#confirm-modal");
  }

  confirm(message, element) {
    // When using Turbo, element can be a form autogenerated, not the source link 
    // If it is the case we try to find the source link to get the list of data attributes
    const pathName = new URL(element.getAttribute("action")).pathname;
    const sourceLink = element.getAttribute("data-turbo-confirm-content") ? element : document.querySelector(`a[href='${pathName}'][data-turbo-confirm='${message}']`);

    this.element.querySelector(".modal-title").textContent = message;
    this.element.querySelector("#custom-body").innerHTML = sourceLink.getAttribute("data-turbo-confirm-content") || message;
    this.element.querySelector("#confirm-button").innerHTML = sourceLink.getAttribute("data-turbo-confirm-action") || "Confirmer";

    this.modal = new Modal(this.element);
    this.modal.show();

    return new Promise((resolve) => {
      this.element.querySelector("#confirm-button").addEventListener("click", () => {
        resolve(true);
      });
    });
  }
}

export default ConfirmModal;